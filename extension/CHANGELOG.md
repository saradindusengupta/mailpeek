# Changelog

## Unreleased

- Initial `.msg` viewer: header card, HTML/plain-text body (with RTF de-encapsulation for messages that only carry a compressed-RTF body), attachment listing with Save, nested `.msg` attachments open in a new tab via a virtual `FileSystemProvider`.
- Copy buttons for individual header fields and a "copy all headers" action.
- `.eml` support not yet implemented — opening a `.eml` file shows a placeholder.
